import React from 'react';

import { Router } from '.';

import '@ionic/react/css/ionic.bundle.css';
import { IonApp } from '@ionic/react';

export const App = () => (
  <IonApp>
    <Router />
  </IonApp>
);
